#include <time.h>
#include <stdlib.h>
#include "minemap.h"

void setMine(char* mineMap[], int row, int col, int mineMax) {
	int minecnt = 0;
	
	srand(time(NULL));
	for (minecnt;minecnt < mineMax;minecnt++) {
		
	}
}